#!/bin/sh

if [ "$ROFI_INFO" != "" ];
then
    coproc ( $ROFI_INFO > /dev/null )
    exit 0
fi

echo -en "\0prompt\x1f  \n" 
echo -en "\0markup-rows\x1ftrue\n"
python $(dirname "$0")/hyprpalette.py
